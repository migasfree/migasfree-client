* Devices management (install/uninstall) (first in server)

* More documentation (requirements, installation, configuration, user manual)

* (un)zip in (un)wrap

* Client as service

* More translation languages

* "packages_excluded" implementation in "upload_computer_info" (first in server)

* Version of API used information
